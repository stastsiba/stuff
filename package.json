{
  "name": "shopstaf",
  "version": "1.0.0",
  "description": "First create project",
  "main": "script.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "json-server --watch db.json"
  },
  "author": "Stas Tsiba",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
